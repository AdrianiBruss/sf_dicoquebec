{% extends "mail.html.twig" %}

{% block content %}
    <h1>{{ email }} a ajouté un nouveau mot au dikkebektionnaire</h1>
    <a href="{{ url("resetPassword", {"email": user.email, "token": user.token}) }}">reset de mot passe</a>
    <p>{{ user.email }}</p>
    <p>{{ user.token }}</p>
{% endblock %}